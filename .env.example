# Example environment variables for opti-frontend (Vite)
# Set the API base used by the app. Include /api if your server routes are under /api
VITE_API_BASE=https://api.optislip.com
# For some tools or CI that use CRA-style vars
REACT_APP_API_BASE=https://api.optislip.com

# Google OAuth Client ID
# Get this from: https://console.cloud.google.com/apis/credentials
# Make sure to add your domain to authorized JavaScript origins
VITE_GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com
